#!/bin/bash
set -eu
CERT=$HOME/.ssl/hickss04.crt
KEY=$HOME/.ssl/hickss04.nocrt.pem
(cd $HOME/apps/sslyze-0_11-linux64/sslyze \
  && python sslyze.py \
  --ca_file $HOME/.ssl/ca-bundle.pem \
  --cert $CERT \
  --key $KEY \
  --regular "$@")
