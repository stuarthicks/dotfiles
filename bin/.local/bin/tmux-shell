#!/bin/sh

if command -v /usr/local/bin/reattach-to-user-namespace >/dev/null 2>&1; then
  export SHELL=/bin/zsh
  exec /usr/local/bin/reattach-to-user-namespace -l /bin/zsh --login
fi

export SHELL=/usr/bin/zsh
exec /usr/bin/zsh --login
